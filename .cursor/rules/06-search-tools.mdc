---
description: Prefer ast-grep for code-aware searches; rg/grep for text/logs/configs; tool priority: ast-grep → rg → grep.
globs:
  - "**/*.{ts,tsx,js,jsx,py,go,rs,java,c,cpp,h,hpp,php,rb,swift,kt,scala,sh,bash,zsh}"
  - "**/*.{json,yaml,yml,toml,xml,html,css,scss,sass,less}"
  - "**/*.{md,mdx,txt,log,conf,config,env}"
  - "**/*"
alwaysApply: true
---

# Search Tools

- Prefer **ast-grep** at `/opt/homebrew/bin/ast-grep` for code-aware searches.
- Use **rg** (ripgrep) or **grep** for plain text/logs/configs/docs or unsupported file types.
- Tool priority: **ast-grep → rg → grep**; choose based on file type/context.

## ast-grep Patterns

```bash
ast-grep --pattern 'function $NAME($ARGS) { $$$ }'
ast-grep --pattern 'class $NAME { $$$ }'
ast-grep --pattern 'import { $ITEMS } from "$MODULE"'
ast-grep --pattern 'const $VAR = $VALUE'
ast-grep --pattern '$OBJ.$METHOD($ARGS)'
```

## Usage

- This rule auto-attaches for all file types; you can also pull it in manually via **@Cursor Rules** when planning refactors, audits, or symbol lookups.
